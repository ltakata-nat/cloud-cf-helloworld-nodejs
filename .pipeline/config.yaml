general:
steps:
  mtaBuild:
    buildTarget: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'naturaeco_CPEA_pocbtpextdevops'
      space: 'dev'
      credentialsId: 'sap_id_temp'
